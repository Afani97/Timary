<doc xmlns="https://hyperview.org/hyperview">
  <screen id="Timer">
    <styles>
        {% include 'mobile/base_styles.xml' %}
        {% include 'mobile/header/_header_styles.xml' with jc="center" %}
        <style id="Flex" flex="1" flexDirection="row" justifyContent="space-between" />
    </styles>
    <body style="Body" id="Body" safe-area="true">
        {% include 'mobile/header/_header.xml' with header_title="Timer" %}
        <view style="Main">
            {% block timer-form %}
                <form id="TimerForm" scroll="true" xmlns="https://hyperview.org/hyperview">
    {% include 'mobile/_errors.xml' with errors=errors.items %}

    <view style="Flex" xmlns:stopwatchs="https://www.usetimary.com/hyperview/">
        <stopwatchs:stopwatch />
    </view>

    <view style="FormGroup">
      <text style="Label">Log to which invoice?</text>
      <picker-field
          field-text-style="Input"
          name="invoice"
          placeholder="Select Invoice"
          value="{{ invoices.first.id }}"
          modal-style="PickerModal"
          modal-text-style="PickerModal__text"
      >
          {% for invoice in invoices %}
            <picker-item label="{{ invoice.title }}" value="{{ invoice.id }}" />
          {% endfor %}
      </picker-field>
    </view>

    <view style="MarginTop20">
        {% url 'mobile:timer' as save_url %}
        {% include 'mobile/inputs/submit_btn.xml' with title="Save" target="TimerForm" url=save_url %}
    </view>


    {% if success %}
    {% include 'mobile/_toast.xml' %}
    {% endif %}
</form>

            {% endblock %}
        </view>
        {% include 'mobile/footer.xml' with active_tab=3 %}
    </body>
  </screen>
  {% include 'mobile/loading_screen.xml' %}
</doc>
