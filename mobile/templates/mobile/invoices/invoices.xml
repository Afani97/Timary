<doc xmlns="https://hyperview.org/hyperview">
  <screen>
    <styles>
        {% include 'mobile/base_styles.xml' %}
        {% include 'mobile/header/_header_styles.xml' with jc="space-between" %}
    </styles>
    <body style="Body" id="Body" safe-area="true">
        {% include 'mobile/header/_new_header.xml' with header_title="Invoices" url="mobile:new_invoices" %}
        <view style="Main">
            {% block invoices %}
                <view xmlns="https://hyperview.org/hyperview"
                      trigger="refresh"
                      href="{% url 'mobile:invoices' %}?partial=true"
                      id="Invoices-List"
                      action="replace-inner">
                    <view style="List" scroll="true" shows-scroll-indicator="false">
                        {% for invoice in invoices %}
                            {% include "mobile/invoices/_invoice.xml" with invoice=invoice %}
                        {% endfor %}
                    </view>
                </view>
            {% endblock %}
        </view>
        {% include 'mobile/footer.xml' with active_tab=2 %}
        <behavior
                trigger="on-event"
                event-name="reload-invoices"
                action="replace-inner"
                target="Invoices-List"
                href="{% url 'mobile:invoices' %}?partial=true"
        />
    </body>
  </screen>
  {% include 'mobile/loading_screen.xml' %}
</doc>
