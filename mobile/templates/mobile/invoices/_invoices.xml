<view xmlns="https://hyperview.org/hyperview"
      trigger="refresh"
      href="{% url 'mobile:invoices' %}?partial=true"
      id="Invoices-List"
      action="replace-inner">
    <view style="List" scroll="true" shows-scroll-indicator="false">
        {% for invoice in invoices %}
            {% include "mobile/invoices/_invoice.xml" with invoice=invoice %}
        {% endfor %}
    </view>
</view>
