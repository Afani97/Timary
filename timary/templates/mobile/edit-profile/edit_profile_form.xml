<form id="EditProfileForm" scroll="true" xmlns="https://hyperview.org/hyperview">
    {% for field, errors in errors.items %}
        {% for error in errors %}
            <text style="Help--Error">{{ field }}: {{ error }}</text>
        {% endfor %}
    {% endfor %}

    <view style="FormGroup">
      <text style="Label">First Name</text>
      <text-field
        value="{{ profile.first_name }}"
        name="first_name"
        placeholder="Tom"
        style="Input"
        placeholderTextColor="#5e6363"
      />
    </view>
    <view style="FormGroup">
      <text style="Label">Last Name</text>
      <text-field
      value="{{ profile.last_name }}"
        name="last_name"
        placeholder="Brady"
        keyboard-type="email-address"
        style="Input"
        placeholderTextColor="#5e6363"

      />
    </view>
    <view style="FormGroup">
      <text style="Label">Email</text>
      <text-field
            value="{{ profile.email }}"
            name="email"
            placeholder="*****"
            style="Input"
            keyboard-type="email-address"
            placeholderTextColor="#5e6363"
      />
    </view>
    <view style="FormGroup">
      <text style="Label">Phone Number</text>
      <text-field
            value="{{ profile.phone_number }}"
            name="phone_number"
            placeholder="+1 333-444-5555"
            style="Input"
            keyboard-type="phone-pad"
            placeholderTextColor="#5e6363"
      />
    </view>
    <view style="MarginTop20">
      <view style="Wrapper">
          <text
              action="replace"
              delay="500"
              href="{% url 'timary:mobile_edit_profile' %}"
              show-during-load="mySpinner"
              style="Update"
              target="EditProfileForm"
              verb="post"
            >
              Update
            </text>
      </view>
    </view>

    {% if success %}
    <behavior trigger="load" action="close" href="#" />
    <behavior trigger="load" action="dispatch-event" event-name="reload" />
    {% endif %}
</form>
